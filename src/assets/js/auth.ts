/*
setTimeout(() => {
    let username = prompt('Podaj login');
    let password = prompt('Podaj hasło');

    game.socket.emit('userLoginData', {
        username: username,
        password: password,
    });
}, 1000);


 */
